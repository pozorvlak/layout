%html
  %head
    %title My Sinatra App

  %body
    %h1 Hello World
    %p Your app is now up and running!
    - if !(y.nil?)
      %p The value of y was #{y}
    - if !(num_hexes.nil?)
      %p The value of num_hexes was #{num_hexes}
